{
  "name": "beachpaws",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for planning better beach days with dog access and water-quality information. Covers every EA-designated bathing water in England plus curated non-designated beaches. No build step; npm is only here to carry the lint/format toolchain.",
  "scripts": {
    "check": "./scripts/check.sh",
    "fix": "biome check --write . && ruff check --fix scripts && ruff format scripts",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.6",
    "html-validate": "11.6.1"
  }
}
