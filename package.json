{
  "name": "beachpaws",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for finding dog-friendly beaches in England. No build step — npm is only here to carry the lint/format toolchain.",
  "scripts": {
    "check": "./scripts/check.sh",
    "fix": "biome check --write . && ruff check --fix scripts && ruff format scripts",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.5",
    "html-validate": "11.5.6"
  }
}
