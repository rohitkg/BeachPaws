.DS_Store

# research staging (subagent rule output, merged into dog_rules.json)
_research/

# npm carries only the lint/format toolchain; the site itself has no build step.
node_modules/

# private planning notes — kept out of the public repo on purpose
PRODUCTION_PLAN.md
